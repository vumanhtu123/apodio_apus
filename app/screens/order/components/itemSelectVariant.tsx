import { Observer, observer } from 'mobx-react-lite';
import { FC } from 'react';
import React, { View } from 'react-native';

export const ItemSelectVariant: FC = observer(
    function ItemSelectVariant() {
        return (
            <View>
                
            </View>
        )
    })